/**
 * Home page — דף הבית
 * Renders the full-screen hero section with daily image and saying.
 */

import HeroSection from '@/components/HeroSection';

export default function HomePage() {
  return <HeroSection />;
}
